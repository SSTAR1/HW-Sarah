var input = prompt("Enter text:").toLocaleLowerCase;
input.split// אולי להוסיף משהו ואז לחלק אותו שיצא מערך שכל אות היא סטרינג בפני עצמה
if (input != a | o | u | i |e ){
    let consonants = ;
    else {
        let vowels = 
    }
}

function vowels_consonants (x);



//output array v + c
function massage(y);
alert(`Your string contains the following vowels|: ${vowels} and the following consonants: ${consonants} `);

//the way to separate the vowels & consonants by chat gpt
/*
function separateVowelsAndConsonants(str) {
  var vowels = "";
  var consonants = "";

  for (var i = 0; i < str.length; i++) {
    var char = str[i].toLowerCase();

    if (isVowel(char)) {
      vowels += char;
    } else if (isConsonant(char)) {
      consonants += char;
    }
  }

  return {
    vowels: vowels,
    consonants: consonants
  };
}

function isVowel(char) {
  return /^[aeiou]$/.test(char);
}

function isConsonant(char) {
  return /^[a-z]$/.test(char) && !isVowel(char);
}

// Example usage
var inputString = "exercise";
var result = separateVowelsAndConsonants(inputString);

console.log("Vowels: " + result.vowels);
console.log("Consonants: " + result.consonants);*/